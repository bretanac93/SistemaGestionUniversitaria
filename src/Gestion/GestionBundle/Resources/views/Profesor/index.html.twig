{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Profesor list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Edad</th>
                <th>Telf</th>
                <th>Adress</th>
                <th>Photo</th>
                <th>Facultad</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('profesor_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombre }}</td>
                <td>{{ entity.edad }}</td>
                <td>{{ entity.telf }}</td>
                <td>{{ entity.adress }}</td>
                <td>{{ entity.photo }}</td>
                <td>{{ entity.facultad }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('profesor_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('profesor_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('profesor_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
